{{

var extractConstructors = function(it) {
  var constructors = [];
  it.extAttrs.forEach(attr => {
    if (attr.name === 'Constructor') {
      constructors.push(attr);
    }
  });
  return constructors;
};


}}