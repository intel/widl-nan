{{~ it.members :p:i }}
{{? p.type === 'operation' }}

{{#def.idlType}}
{{#def.argument}}

  {{=idlType2CxxType(p.idlType, it.refTypeMap)}} {{=p.name}}({{=generateParameterList(p, it.refTypeMap)}});
{{?}}
{{~}}
