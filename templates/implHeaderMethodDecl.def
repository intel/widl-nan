{{~ it.members :p:i }}
{{? p.type === 'operation' }}

{{#def.returnType}}
{{#def.argument}}

  {{=idlType2CxxReturnType(p.idlType, it.refTypeMap)}} {{=p.name}}({{=generateParameterList(p, it.refTypeMap)}});
{{?}}
{{~}}
