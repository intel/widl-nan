{{~ it.members :p:i }}
{{? p.type === 'operation' }}

{{#def.idlType2CxxConversion}}
{{#def.methodArgument}}

  {{=ConvertIDLType2Cxx(p.idlType, it.refTypeMap)}} {{=p.name}}({{=generateParameterList(p, it.refTypeMap)}});
{{?}}
{{~}}
