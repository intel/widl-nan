{{#def.propertyReturnType}}

{{~ it.members :p:i }}

{{? p.type === 'attribute' && p.static === false }}
  {{=idlType2CxxImplPrivateDataMemberType(p.idlType, it.refTypeMap)}} {{=p.name}}_;
{{?}}

{{~}}
