{{~ it.members :p:i }}
{{? p.type === 'attribute' }}

{{#def.idlType2CxxConversion}}
{{var propertyType = ConvertIDLType2Cxx(p.idlType);}}

  const {{=propertyType}} get_{{=p.name}}() const {
    return this->{{=p.name}}_;
  }

{{? ! p.readonly }}

  void set_{{=p.name}}(const {{=propertyType}}& new_value) {
    this->{{=p.name}}_ = new_value;
  }

{{?}}

{{?}}
{{~}}
