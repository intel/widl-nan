{{~ it.members :p:i }}
{{? p.type === 'attribute' }}

{{#def.idlType}}
{{var propertyType = idlType2CxxType(p.idlType, it.refTypeMap);}}

  {{=propertyType}} get_{{=p.name}}() const {
    return this->{{=p.name}}_;
  }

{{? ! p.readonly }}

  void set_{{=p.name}}(const {{=propertyType}}& new_value) {
    this->{{=p.name}}_ = new_value;
  }

{{?}}

{{?}}
{{~}}
