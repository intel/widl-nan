{{~ it.members :p:i }}
{{? p.type === 'operation' }}

{{#def.idlType2CxxConversion}}
{{#def.methodArgument}}

{{=ConvertIDLType2Cxx(p.idlType)}} {{=it.name}}::{{=p.name}}({{=generateParameterList(p)}}) {
  // TODO: fill your code here
  
}

{{?}}
{{~}}
