{{#def.returnType}}
{{#def.argument}}
{{#def.callback}}

{{
  var callback = extractAllCallbackMethod(it);
}}


{{~ it.members :p:i }}
{{? p.type === 'operation' }}


{{=idlType2CxxReturnType(p.idlType, it.refTypeMap)}} {{=it.name}}::{{=p.name}}({{=generateParameterList(p, it.refTypeMap)}}) {
  {{=getCallbackHelperAssignmentString(p, '  ')}}

  // TODO: fill your code here

}

{{?}}
{{~}}
