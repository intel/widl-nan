{{#def.callback}}
{{#def.argument}}
{{#def.className}}
{{#def.extractConstructors}}

{{
var className = it.name;
}}

{{ var headerFileName = className.toLowerCase(); }}
#include "{{=headerFileName}}.h"



{{ var constructors = extractConstructors(it); }}

{{? constructors.length }}

{{~ constructors :p:i }}
{{=className}}::{{=className}}({{=generateParameterList(p, it.refTypeMap)}}) {
  // TODO: init your members
}
{{~}}

{{?? true}}
{{=className}}::{{=className}}() {
  // TODO: init your members

}
{{?}}


{{=className}}::~{{=className}}() {
  // TODO: do cleanup if necessary

}

{{#def.implCppMethod}}
